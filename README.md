# Random Walk Metropolis–Hastings: MCMC Sampling & Convergence Diagnostics

A **from-scratch implementation** of the **Random Walk Metropolis (RWM)** algorithm—a **Markov Chain Monte Carlo (MCMC)** method—for sampling from a **Laplace distribution** and assessing convergence using the **Gelman–Rubin (\(R_b\)) diagnostic**.

> **Language:** Python  
> **Libraries:** NumPy, Matplotlib, Seaborn

---

## Overview

Many probability distributions cannot be sampled from directly using standard techniques. Markov Chain Monte Carlo (MCMC) methods overcome this by constructing a Markov chain whose long-run behaviour converges to the desired target distribution.

This project implements the **Random Walk Metropolis algorithm** from scratch to generate samples from the Laplace distribution

\[
f(x)=\frac{1}{2}e^{-|x|}
\]

and evaluates the quality of the sampler using Monte Carlo estimation and convergence diagnostics.

---

## Project Objectives

This project:

- Implements the **Random Walk Metropolis (RWM)** algorithm from scratch.
- Generates samples from a **Laplace distribution** without direct sampling.
- Validates the sampler using histograms, kernel density estimation (KDE), and Monte Carlo estimates.
- Assesses convergence using multiple independent Markov chains and the **Gelman–Rubin (\(R_b\)) diagnostic**.
- Investigates how the proposal step size affects convergence and mixing behaviour.

  ## How the Random Walk Metropolis Algorithm Works

At every iteration:

1. Propose a new candidate point near the current position.
2. Compare how likely the proposed point is under the target distribution.
3. Accept or reject the proposal according to the Metropolis acceptance rule.
4. Repeat thousands of times.

```text
for i = 1,...,N:

    propose x* ~ Normal(x[i-1], s)

    r = f(x*) / f(x[i-1])

    u ~ Uniform(0,1)

    if u < r:
        x[i] = x*
    else:
        x[i] = x[i-1]
```

To improve numerical stability, the implementation performs the comparison on the **log scale**:

```text
log(u) < log f(x*) − log f(x[i−1])
```

instead of

```text
u < f(x*) / f(x[i−1])
```

which avoids floating-point underflow.

---

# Results

## 1. Sampling Quality

Using

- **N = 10,000**
- **Proposal standard deviation (s) = 1**

the generated histogram and kernel density estimate closely match the analytical Laplace density.

The Monte Carlo estimates were approximately :

| Statistic | Estimate | True Value |
|----------|---------:|-----------:|
| Mean | -0.028 | 0 |
| Standard Deviation | 1.407 | √2 ≈ 1.414 |

Note : The value differs slightly everytime the notebook is rerun, but are apporximately the same.


---

## 2. Convergence Analysis

Four independent chains were started from widely separated initial values.

The Gelman–Rubin diagnostic showed:

- **s = 0.001:** \(R_b \approx 418\)
  - Chains barely moved.
  - No convergence.

- As **s** increased:
  - Chains mixed more effectively.
  - \(R_b\) rapidly decreased.

- Around **s ≈ 0.28**, the diagnostic crossed the commonly used convergence threshold of **1.05**.

- By **s ≈ 1**, \(R_b \approx 1.00\), indicating successful convergence.



---

## Key Takeaways

- Small proposal step sizes lead to **poor mixing** and slow convergence.
- Larger proposal step sizes improve exploration of the target distribution.
- The **Gelman–Rubin (\(R_b\)) diagnostic** provides a simple and effective way to assess convergence before relying on Monte Carlo estimates.

---

# Repository Structure

| File | Description |
|------|-------------|
| `01_metropolis_hastings_sampling.ipynb` | Implements the Random Walk Metropolis algorithm, generates samples, visualises the target distribution, and computes Monte Carlo estimates. |
| `02_convergence_diagnostics.ipynb` | Runs multiple Markov chains, computes the Gelman–Rubin (\(R_b\)) statistic, and analyses the effect of proposal step size. |
| `figures/` | Plots generated by the notebooks. |
| `requirements.txt` | Python dependencies. |

---

# Installation

Clone the repository:

```bash
git clone https://github.com/yourusername/metropolis-hastings-laplace.git
cd metropolis-hastings-laplace
```

Install dependencies:

```bash
pip install -r requirements.txt
```

Launch Jupyter Notebook:

```bash
jupyter notebook
```

Run the notebooks in the following order:

1. `01_metropolis_hastings_sampling.ipynb`
2. `02_convergence_diagnostics.ipynb`

Both notebooks are fully self-contained and require no external datasets.

---

# Requirements

- Python 3.9+
- NumPy
- Matplotlib
- Seaborn

---

## Skills Demonstrated

- Markov Chain Monte Carlo (MCMC)
- Metropolis–Hastings Algorithm
- Monte Carlo Simulation
- Probability Distributions
- Kernel Density Estimation
- Convergence Diagnostics
- Statistical Computing
- Python (NumPy, Matplotlib, Seaborn)

  
